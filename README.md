# PropertyDealDesk Remote Access Control

This repository hosts the lightweight remote access control JSON used by PropertyDealDesk Match Edition clients.

Do not store secrets, tokens, source code, customer private data, or operational credentials in this repository.

Match Edition reads `remote-access.json` from the configured global raw GitHub URL. Access is controlled by each installation's `clientId`.

